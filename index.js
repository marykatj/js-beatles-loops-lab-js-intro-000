function theBeatlesPlay(musician, instrument) {
  var empty = [];
  for (let i = 0; i < 4; i) {
     return empty.push(`${musician} plays ${instrument}.`);
    }
}

const facts = [];
function johnLennonFacts(facts) {
  let i = 0
  while (i < facts.length) {
    var newArray = facts[i] +"!!!"
    ++i;
  }
  return newArray;
}
